services:
  eleventy:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.dev
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
